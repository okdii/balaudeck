• New: Data Transfer — copy tables from one connection to another. Right-click any database, pick a target connection and database, choose structure and/or data for each table, and copy directly. Live progress with pause and cancel, a searchable target-database picker, and per-table row counts. Works for MySQL/MariaDB, PostgreSQL, SQL Server and SQLite
• Data Transfer streams row by row with no temporary file, so even very large tables copy with low memory use, and each batch is sized to what the destination accepts
• Faster SQL import — multi-row batching and bulk-load tuning make imports and copies dramatically quicker
• SQL Server date and time values now export and display correctly
• A readable Query Plan (EXPLAIN) view, a Refresh button on the database tree, and a bigger terminal font (up to 40px) with much longer scrollback
