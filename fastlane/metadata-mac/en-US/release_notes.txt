• New: a built-in AI assistant. Open it beside any SSH session, SQL database or local terminal — ask in plain language and it runs the read-only commands or queries itself, then asks your approval before anything that changes state. Bring your own Claude or OpenAI-compatible key, or run a model locally with Ollama (no key, no per-token cost). Answers are formatted, and the panel is resizable
• PostgreSQL, SQL Server and SQLite now match MySQL/MariaDB — edit query results inline, click through foreign keys, use the visual table designer (create, alter, drop, show DDL, create database), and run native dump + import
• User & privilege management — a Users panel for MySQL/MariaDB, PostgreSQL and SQL Server: create and edit users and roles, and set privileges and object grants
• SFTP folders — upload several files at once, upload a whole folder, and download an entire folder, all recursively
• Fixes: the terminal is reset cleanly on each SSH reconnect, the tmux mouse toggle behaves, and the host picker is no longer clipped
