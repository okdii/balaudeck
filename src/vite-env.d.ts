/// <reference types="vite/client" />

/** Short git commit the frontend was built from (injected by vite.config.ts). */
declare const __BUILD_HASH__: string;

/** Whether the self-updater UI is compiled in (false for store builds). */
declare const __UPDATER_ENABLED__: boolean;
