# BalauDeck — App Store / Play store listing

Copy these into App Store Connect (and Google Play later). Limits noted.

## App name
BalauDeck

## Subtitle (App Store, ≤30 chars)
SSH, SFTP, tunnels & MySQL

## Promotional text (App Store, ≤170 chars)
One app for your servers: SSH terminal, SFTP files, port-forward tunnels, and a
MySQL/MariaDB client — with full-screen panes and encrypted cross-device sync.

## Keywords (App Store, ≤100 chars, comma-separated)
ssh,sftp,terminal,mysql,mariadb,database,tunnel,port forward,client,server,devops,console,admin

## Description
BalauDeck is an all-in-one client for the servers you already run — SSH, SFTP,
SSH tunnels, and MySQL/MariaDB — in one fast, native app for iPhone, iPad and Mac.

• SSH terminal — a real interactive shell (xterm), password or key auth, host-key
  verification, and an on-screen key bar for the keys phones don't have.
• SFTP browser — upload, download, rename, delete, mkdir, and change permissions
  (chmod), with an option to browse elevated.
• SSH tunnels — forward a local port through a server to reach an internal service.
• MySQL/MariaDB client — schema browser, SQL editor with highlighting, a results
  grid you can edit inline, table designer, and SQL import/export.
• Split panes & full screen — arrange several sessions side by side, or maximize
  one to fill the display.
• Resilient connections — keepalive detects drops, with reconnect and optional
  tmux session persistence so you pick up exactly where you left off.
• Cross-device sync — export an encrypted, passphrase-protected bundle of your
  connections and import it on another device. Nothing is stored on our servers.

Your connections and credentials stay on your device (passwords and keys live in
the system keychain). BalauDeck has no account, no tracking, and no ads.

## Support URL
https://okdii.com/balaudeck

## Marketing URL (optional)
https://okdii.com/balaudeck

## Privacy Policy URL
https://okdii.com/balaudeck/privacy

## Category
Primary: Developer Tools · Secondary: Utilities

## Age rating
4+

## App Privacy (App Store) / Data safety (Play)
Data Not Collected — the app collects no data. (No tracking, no third-party SDKs.)

## Export compliance
Uses only standard encryption (AES + TLS/SSH) → exempt.
Info.plist: ITSAppUsesNonExemptEncryption = false (already set).
